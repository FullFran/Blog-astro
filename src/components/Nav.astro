---
import { NAV_ITEMS } from '$/config'
---
<!--
<nav class="nav-main w-full flex justify-center mt-2 mb-4">
  <ul class="flex gap-4 md:gap-8 items-center text-base font-medium">
    {Object.entries(NAV_ITEMS).map(([key, item]) => (
      <li key={key}>
        <a 
          href={item.path}
          class="nav-link px-2 py-1 rounded transition-colors duration-150
                text-cyan-700 dark:text-cyan-300 hover:bg-cyan-100 dark:hover:bg-cyan-900/50 hover:text-cyan-900 dark:hover:text-cyan-100"
        >
          {item.title}
        </a>
      </li>
    ))}
  </ul>
</nav>

<style>
  .nav-link {
    @apply focus:outline-none focus:ring-2 focus:ring-cyan-400;
  }
</style>
-->